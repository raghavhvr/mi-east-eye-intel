# AI Brief generation (add to GitHub Secrets, NOT here)
# Primary — free at console.groq.com
GROQ_API_KEY=gsk_...

# Fallback if Groq hits rate limit — free at aistudio.google.com
GEMINI_API_KEY=AIza...

# ACLED conflict data — register at acleddata.com, then use your login credentials
# ACLED now uses OAuth (email + password → Bearer token, refreshed each run)
ACLED_EMAIL=your@email.com
ACLED_PASSWORD=your_password
